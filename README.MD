Adicionar a entrada abaixo ao arquivo `C:\Windows\System32\drivers\etc\hosts`, no Windows, e executar o comando `ipconfig /flushdns`, para atualizar as entradas de DNS da máquina.

```
127.0.0.1 api-app.localhost
```